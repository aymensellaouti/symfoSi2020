{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    <div class="row">
        <div class="col">{{ form_row(form.name) }}</div>
        <div class="col">{{ form_row(form.firstname) }}</div>
    </div>
    {{ form_rest(form) }}
    {{ form_end(form) }}

{% endblock %}

